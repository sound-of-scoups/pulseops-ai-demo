# PulseOps AI 增长编排台

## 第一次运行

先确认电脑已经安装 Python 3.10+、Node.js 18+。

1. 双击当前目录下的 `启动演示.bat`。
2. 等待两个 PowerShell 窗口出现服务启动信息。
3. 浏览器打开 `http://localhost:5173/`。
4. 使用完毕后，关闭两个 PowerShell 窗口，服务才会停止。

如果第一次启动提示缺少依赖，在项目目录打开 PowerShell，依次执行：

```powershell
cd backend
python -m pip install -r requirements.txt
cd ..\frontend
npm install
```

## 查看什么

- `http://localhost:5173/`：完整可视化工作台，主要看这里。
- `http://127.0.0.1:8000/docs`：FastAPI 接口文档。
- `http://127.0.0.1:8000/health`：后端健康检查，只会显示 JSON。

## 演示建议

- 点击“618 高价值客群”：查看 SQL、清洗规则、自动重试、A/B 文案和 5 项测试。
- 点击“跨境合规护栏”：查看文案敏感词拦截。
- 点击“核心资产 HITL”：查看高危任务挂起，再点击“批准复苏”或“驳回并熔断”。

## 项目不是生产系统

当前版本是离线教学演示：数据库查询、Agent 输出和样本数据由后端沙盒确定性模拟，不会连接真实生产数据库，也不会真的发送营销消息。后续接入真实 LLM、数仓和营销平台时，需要替换 `backend/app/agents/` 下的模拟实现。

## 发布成别人可以访问的网站

最简单的方式是使用 Render：

1. 在 GitHub 新建一个仓库，把 `ai_augmented_training` 目录中的全部文件上传进去。
2. 登录 Render，选择 `New` -> `Blueprint`，选择这个 GitHub 仓库。
3. Render 会自动读取当前目录的 `render.yaml`，安装依赖、构建 Vue、把 Vue 页面交给 FastAPI，并生成一个 `onrender.com` 公网网址。
4. 把这个公网网址发到邮箱，老师或同学可以直接打开。

Render 的免费服务可能会在一段时间无人访问后休眠，第一次打开需要等待几十秒唤醒。这个演示不需要数据库和 API Key，因此适合提交作业。
